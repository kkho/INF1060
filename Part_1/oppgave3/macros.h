#ifndef MACROS_H
#define MACROS_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define max(a,b) (((a) > (b)) ? (a) : (b))
#define min(a,b) (((a) < (b)) ? (a) : (b))
#define compare_to(a,b) (strcmp(a,b) == 0)
#define error -1
#define true 1
#define false 0
#define inc(k) k = k +1


#endif

